@page
@using Microsoft.AspNetCore.Mvc.TagHelpers
@using comaiz.data.Models
@model comaiz.Pages.FixedCosts.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>FixedCost</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="FixedCost.Contract" class="control-label"></label>
                <select asp-for="FixedCost.ContractId" class="form-control"
                        asp-items="@Model.ContractsNameSelectList">
                    <option value="">-- Select ClientContract --</option>
                </select>
                <span asp-validation-for="FixedCost.ContractId" class="text-danger" />
            </div>
            <div class="form-group">
                <label asp-for="FixedCost.Name" class="control-label"></label>
                <input asp-for="FixedCost.Name" class="form-control" />
                <span asp-validation-for="FixedCost.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FixedCost.Amount" class="control-label"></label>
                <input asp-for="FixedCost.Amount" class="form-control" />
                <span asp-validation-for="FixedCost.Amount" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
