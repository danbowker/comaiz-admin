@page
@model comaiz.Pages.FixedCosts.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>FixedCost</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="FixedCost.Id" />
            <div class="form-group">
                <label asp-for="FixedCost.ContractId" class="control-label"></label>
                <select asp-for="FixedCost.ContractId" class="form-control" asp-items="ViewBag.ContractId"></select>
                <span asp-validation-for="FixedCost.ContractId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FixedCost.Name" class="control-label"></label>
                <input asp-for="FixedCost.Name" class="form-control" />
                <span asp-validation-for="FixedCost.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
